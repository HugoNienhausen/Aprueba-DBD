{
  "name": "app-dbd",
  "private": true,
  "description": "App de repaso DBD (Bases de Datos)",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "typecheck": "npm run typecheck --prefix web",
    "dev:clean": "npm run dev:clean --prefix web",
    "sync-data": "python3 scripts/sync_to_web.py",
    "generate-explanations": "python3 scripts/generate_explanations.py",
    "validate-data": "node scripts/validate_questions_json.js"
  }
}
